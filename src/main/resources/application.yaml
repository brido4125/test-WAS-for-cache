server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://61.97.185.90:3306/test
    username: root
    password: hcshcs
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  level:
    org:
      hibernate: info